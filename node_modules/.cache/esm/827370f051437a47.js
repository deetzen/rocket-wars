let PowerUp,Skin;_514‍.x([["default",()=>_514‍.o]]);_514‍.w("./PowerUp",[["default",["PowerUp"],function(v){PowerUp=v}]]);_514‍.w("../../skin/Skin",[["default",["Skin"],function(v){Skin=v}]]);


class PermanentFire extends PowerUp {
  constructor (stage, options) {
    super(stage, options);
    this.skin = new Skin('powerup-permanentfire', 1, 2, 15);
    this.interval = null;
  }

  startFire (object) {
    this.interval = setInterval(() => {
      if (object.player) {
        object.fire();
        object.player.ammo += 1;
      }
    }, 180);
  }

  hit (object) {
    if (this.visible === true) {
      if (object.player && object.player.character) {
        this.startFire(object.player.character);
      }
    }
    super.hit();
    this.game.sound.play('powerup-permanentfire', true);
  }

  remove () {
    clearInterval(this.interval);
    super.remove();
  }
}

_514‍.d(PermanentFire);
