let PLAY_SOUND;_0b3‍.x([["default",()=>Sound]]);_0b3‍.w("../../events",[["PLAY_SOUND",["PLAY_SOUND"],function(v){PLAY_SOUND=v}]]);

               class Sound {
  constructor () {
    this.io = null;
    this.socket = null;
  }

  setSocket (socket) {
    this.socket = socket;
  }

  setIo (io) {
    this.io = io;
  }

  play (sound, privateSound = false) {
    if (privateSound) {
      this.socket.emit(PLAY_SOUND, sound);
    } else {
      this.io.emit(PLAY_SOUND, sound);
    }
  }
}
