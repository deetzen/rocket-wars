let Asteroid,Skin;_249‍.x([["default",()=>_249‍.o]]);_249‍.w("./Asteroid",[["default",["Asteroid"],function(v){Asteroid=v}]]);_249‍.w("../../skin/Skin",[["default",["Skin"],function(v){Skin=v}]]);


class Planet extends Asteroid {
  constructor (stage, options) {
    super(stage, options);
    this.velocity = Math.round(Math.random() * 0.4) + 0.2;
    this.mass = 500;
    this.zIndex = 0;
    this.size = Math.round(Math.random() * 300) + 300;
    this.alive = false;

    const index = Math.round(Math.random() * 3) + 1;

    this.skin = new Skin(`planet-${index}`);
  }

  rotate () {
    this.rotation += this.rotateDirection * 0.1;
  }

  hit () {
    return this;
  }
}

_249‍.d(Planet);
